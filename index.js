/* jshint node: true */

var run = require('./lib/run');
var list = require('./lib/list');
var report = require('./lib/report');

module.exports = {
    run: run,
    list: list,
    report: report
};
